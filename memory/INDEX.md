# Memory Index

<!-- One line per memory file, nothing else — this index auto-loads every session
     (via CLAUDE.md), so every line here is a permanent context cost. Memory CONTENT
     never goes in this file. Conventions: memory/README.md. -->

- (empty — your first "remember this" creates a file here. See
  `examples/summit-digital/memory/` for what an entry looks like.)
